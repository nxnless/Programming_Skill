#include <iostream>
#include <vector>

using namespace std;

int main() {
    int R, X, Y, T;
    cin >> R >> X >> Y >> T;

    // ??????????????????????
    vector<vector <char> > map(X + 4, vector<char>(Y + 4, '.'));

    // ??????????????????????????????????
    for (int i = 0; i < T; ++i) {
        int x, y;
        cin >> x >> y;
        map[x][y] = '*'; // '*' ??????????????????????
    }

    int x_start, y_start, x_target, y_target;
    // ?????????????????????????
    cin >> x_start >> y_start >> x_target >> y_target;

    // TODO: ?????????????????????????????

    // TODO: ???????????
    // ???????????????????????????????????
bool canReach = true;
for (int i = x_start - 1; i <= x_start + 1 && canReach; ++i) {
    for (int j = y_start - 1; j <= y_start + 1 && canReach; ++j) {
        if (map[i][j] == '*') {
            canReach = false;
        }
    }
}

// TODO: ??????????? Yes ???? No ????????????????
// ?????????????????????
for (int i = 2; i <= X + 1; ++i) {
    for (int j = 2; j <= Y + 1; ++j) {
        if (map[i][j] == '*') {
            // ????????????
            // ????????????????????????????????????
        } else if (map[i][j] == '.') {
            // ???????????
            // ????????????????????????????????????
        }
    }
}
// ??????????? Yes ???? No ????????????????
cout << (canReach ? "Yes" : "No") << endl;

// ?????????????????????
for (int i = 2; i <= X + 1; ++i) {
    for (int j = 2; j <= Y + 1; ++j) {
        cout << map[i][j];
    }
    cout << endl;
}

    return 0;
}

